# components/ui

Design system primitives aligned with `docs/branding/Website_Design_System.md`.

Stateless, token-driven components: Button, Badge, Card, Input, Textarea, Select, Accordion, Chip.

No imports from `sections/` or `pages/`.
