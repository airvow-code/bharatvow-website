# components/layout

Site chrome used inside `layouts/MainLayout.jsx`.

Header, Footer, primary nav, Solutions mega-menu, mobile drawer, optional sticky Download bar (one saffron CTA per viewport on mobile).
