# src/assets

Vite-processed assets imported from JSX/JS (`import logo from '@/assets/...'`).

Use for small files that benefit from bundling (SVG icons, inline images). Large screenshot libraries live in `website/assets/screenshots/` at project root.
