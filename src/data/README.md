# data (supporting folder)

Static content modules sourced from `docs/website/`. Not in the core folder list but used by pages.

Content modules for live routes — see `src/data/` per page (home, faq, legal, digitalToolPages, etc.).
