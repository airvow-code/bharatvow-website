# hooks

Reusable React hooks for UI behaviour.

Planned: `useMediaQuery`, `useScrollPosition`, `useReducedMotion`, `useStickyCTA`.

No page-specific business logic — keep hooks generic and testable.
