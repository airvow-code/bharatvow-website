# layouts

Page shells wrapping React Router `<Outlet />`.

| Layout | Use |
|--------|-----|
| `MainLayout` | Default site — nav + main + footer |
| `LegalLayout` | (Future) Narrow prose column for legal docs |
| `MinimalLayout` | (Future) Standalone download / error pages |

Layouts define structure only, not page content.
