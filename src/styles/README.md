# styles

Global CSS and design tokens.

| File | Purpose |
|------|---------|
| `variables.css` | `--bv-*` tokens from Website Design System |
| `global.css` | Reset, typography, container, section spacing |
| `typography.css` | (Future) Prose / legal styles |
| `animations.css` | (Future) Motion + `prefers-reduced-motion` |

Component styles stay co-located unless shared by 3+ components.
