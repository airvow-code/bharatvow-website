/**
 * Homepage section toggles.
 *
 * Set `testimonials: true` when verified customer quotes are available post-launch,
 * then uncomment is automatic via Home.jsx.
 */
export const HOME_SECTIONS = {
  testimonials: false,
};
